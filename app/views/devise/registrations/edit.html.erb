<h2>Редактирование профиля</h2>

<%= simple_form_for resource, :url => registration_path(resource_name), :method => :put do |f| %>
  <%= f.input :surname %>
  <%= f.input :name %>
  <%= f.input :patronymic %>
  <%= f.input :email %>
  <%= f.input :password, :hint => 'Оставьте пустым, если не хотите менять пароль' %>
  <%= f.input :password_confirmation %>
  <%= f.input :current_password %>
  <%= f.submit 'Изменить' %>
<% end %>

<h3>Связанные аккаунты</h3>
Для запрещения доступа с помощью социальной сети к учетной записи ТУСУР - необходимо удалить авторизованное приложение в настройках конкретной социальной сети!
<ul>
  <% resource.identities.each do |identity| %>
    <li>
      <%= render :partial => 'identities/identity', :object => identity %>
      <%= link_to 'Удалить', user_identity_path(resource, identity),
                  :data => { :confirm => 'Вы точно, хотите удалить этот аккаунт?' },
                  :method => :delete %>
    </li>
  <% end %>
</ul>

<ul>
  <% resource_class.omniauth_providers.each do |provider| %>
    <li>
      <%= link_to "Связать #{provider.to_s.titleize}", omniauth_authorize_path(resource_name, provider) %>
    </li>
  <% end %>
</ul>

<h3>Отмена учетной записи</h3>

<%= link_to 'Удалить мою учетную запись', registration_path(resource_name),
            :data => { :confirm => 'Вы точно, хотите удалить вашу учетную запись?' },
            :method => :delete %></p>

<%= link_to 'Назад', :back %>
