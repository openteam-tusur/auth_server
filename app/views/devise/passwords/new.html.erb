<h2>Восстановление пароля</h2>

<%= simple_form_for resource, :url => password_path(resource_name), :method => :post do |f| %>
  <%= f.input :email, :autofocus => true %>
  <%= f.submit 'Выслать письмо с инструкциями восстановления' %>
<% end %>

<%= render "devise/shared/links" %>
